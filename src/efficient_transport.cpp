#include <iostream>
#include "libs/container.hpp"

int main()
{
    // Fast reading from input
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0);

    Container container;

    container.PrintVars();

    return 0;
}
